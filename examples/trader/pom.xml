<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jbehave</groupId>
    <artifactId>jbehave-examples</artifactId>
    <version>3.0-SNAPSHOT</version>
  </parent>
  <artifactId>jbehave-trader-example</artifactId>
  <name>JBehave Trader Example</name>

  <dependencies>
    <dependency>
      <groupId>org.jbehave</groupId>
      <artifactId>jbehave-pico</artifactId>
      <version>${jbehave.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jbehave</groupId>
      <artifactId>jbehave-spring</artifactId>
      <version>${jbehave.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jbehave</groupId>
      <artifactId>jbehave-guice</artifactId>
      <version>${jbehave.version}</version>
    </dependency>
  </dependencies>
  <build>
    <resources>
      <resource>
        <directory>${basedir}/src/main/java</directory>
        <filtering>false</filtering>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-report-resources</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <overWriteReleases>false</overWriteReleases>
              <overWriteSnapshots>true</overWriteSnapshots>
              <artifactItems>
                <artifactItem>
                  <groupId>org.jbehave.site</groupId>
                  <artifactId>jbehave-site-resources</artifactId>
                  <version>2.0.2</version>
                  <outputDirectory>${project.build.directory}/jbehave-reports/rendered</outputDirectory>
                </artifactItem>
                <artifactItem>
                  <groupId>org.jbehave</groupId>
                  <artifactId>jbehave-core</artifactId>
                  <version>${jbehave.version}</version>
                  <outputDirectory>${project.build.directory}/jbehave-reports/rendered</outputDirectory>
                  <includes>**\/*.css,**\/*.ftl,**\/*.js</includes>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jbehave</groupId>
        <artifactId>jbehave-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>runnable-stories</id>
            <phase>integration-test</phase>
            <configuration>
              <storyIncludes>
                <storyInclude>**/stories/*.java</storyInclude>
              </storyIncludes>
              <storyExcludes />
              <skip>${skip.runnable.stories}</skip>
              <batch>${batch.mode}</batch>
              <ignoreFailureInStories>true</ignoreFailureInStories>
              <ignoreFailureInReports>${ignore.failure.reports}</ignoreFailureInReports>
              <renderReportsAfterStories>true</renderReportsAfterStories>
            </configuration>
            <goals>
              <goal>run-stories</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>report-failures</id>
      <properties>
        <ignore.failure.reports>false</ignore.failure.reports>
      </properties>
    </profile>
    <profile>
      <id>batch-mode</id>
      <properties>
        <batch.mode>true</batch.mode>
      </properties>
    </profile>
    <profile>
      <id>stories-as-paths</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jbehave</groupId>
            <artifactId>jbehave-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>stories-as-paths</id>
                <phase>integration-test</phase>
                <configuration>
                  <storyEmbedder>org.jbehave.examples.trader.ClasspathTraderStoryEmbedder</storyEmbedder>
                  <storyIncludes>
                    <storyInclude>**/stories/*.story</storyInclude>
                  </storyIncludes>
                  <storyExcludes />
                  <skip>false</skip>
                  <batch>${batch.mode}</batch>
                  <ignoreFailureInStories>true</ignoreFailureInStories>
                  <ignoreFailureInReports>${ignore.failure.reports}</ignoreFailureInReports>
                  <renderReportsAfterStories>true</renderReportsAfterStories>
                </configuration>
                <goals>
                  <goal>run-stories-as-paths</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
      <properties>
        <skip.runnable.stories>true</skip.runnable.stories>
      </properties>
    </profile>
    <profile>
      <id>stepdoc</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jbehave</groupId>
            <artifactId>jbehave-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>stepdoc</id>
                <phase>integration-test</phase>
                <configuration>
                  <storyEmbedder>org.jbehave.examples.trader.ClasspathTraderStoryEmbedder</storyEmbedder>
                </configuration>
                <goals>
                  <goal>stepdoc</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>ant</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <id>run-stories-via-ant</id>
                <phase>integration-test</phase>
                <configuration>
                  <tasks>
                    <taskdef name="storyRunner"
                      classname="org.jbehave.ant.StoryRunnerTask"
                      classpathref="maven.runtime.classpath" />
                    <storyRunner storyIncludes="**/stories/*.java"
                      skip="false" ignoreFailureInStories="true"
                      ignoreFailureInReports="${ignore.failure.reports}"
                      renderReportsAfterStories="true" />
                    <taskdef name="stepdoc" classname="org.jbehave.ant.StepdocTask"
                      classpathref="maven.runtime.classpath" />
                    <stepdoc
                      storyEmbedder="org.jbehave.examples.trader.ClasspathTraderStoryEmbedder"
                      skip="false" />
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
      <properties>
        <skip.runnable.stories>true</skip.runnable.stories>
      </properties>
    </profile>
  </profiles>
  <properties>
    <batch.mode>false</batch.mode>
    <ignore.failure.reports>true</ignore.failure.reports>
    <skip.runnable.stories>false</skip.runnable.stories>
  </properties>

</project>
