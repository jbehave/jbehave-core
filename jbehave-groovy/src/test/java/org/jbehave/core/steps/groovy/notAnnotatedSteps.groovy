class NotAnnotatedSteps {

  def aDate(Date date) {
    assertNotNull(date);
  }

  def daysPass(int days) {
    assertNotNull(days);
  }

  def theDate(Date date) {
      assertNotNull(date);
  }

}
